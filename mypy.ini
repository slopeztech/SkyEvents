# mypy.ini — standalone mypy configuration (mirrors pyproject.toml for IDE compatibility)
# This file is here for editors that read mypy.ini directly.
# The authoritative config is in pyproject.toml [tool.mypy]
